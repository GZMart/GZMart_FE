import CampaignsPage from '../../seller/CampaignsPage';

/**
 * Admin: giám sát Flash Sale toàn sàn (xem, tạm dừng, dừng, gỡ).
 * Tạo/sửa giá deal chỉ trong trang seller — đúng thông lệ sàn.
 */
const AdminFlashCampaignsPage = () => <CampaignsPage mode="admin" />;

export default AdminFlashCampaignsPage;
